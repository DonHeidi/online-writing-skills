{
  "name": "online-writing",
  "version": "2.8.1",
  "private": true,
  "description": "Release tooling for the online-writing plugin. The plugin itself ships from skills/ and the manifests under .claude-plugin/ and .codex-plugin/; this package.json exists only to give changesets a single source of truth for the version and to host the release scripts.",
  "license": "MIT",
  "author": {
    "name": "Sebastian Heitmann",
    "url": "https://github.com/DonHeidi"
  },
  "repository": "https://github.com/DonHeidi/online-writing-skills",
  "scripts": {
    "changeset": "changeset",
    "version-packages": "changeset version && node scripts/sync-version.mjs",
    "release": "changeset tag"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  }
}
